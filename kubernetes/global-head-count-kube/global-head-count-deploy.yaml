apiVersion: apps/v1
kind: Deployment
metadata:
  name: global-head-count-deploy
  labels:
    app.kubernetes.io/name: global-head-count-app
    app.kubernetes.io/instance: global-head-count-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: global-head-count-app
      app.kubernetes.io/instance: global-head-count-deploy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: global-head-count-app
        app.kubernetes.io/instance: global-head-count-deploy
    spec:
      containers:
        - name: global-head-count-app
          image: global-head-count:global
          resources:
            requests:
              memory: "64Mi" # 64 MebiBytes = 67.1089 Megabytes
              cpu: "250m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 8003