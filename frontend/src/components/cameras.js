function Cameras({ totalCount }) {
  console.log(totalCount);
  return (
    <div className="text bg-dark" style={{ height: "100%" }}>
      Total Count: {totalCount}
    </div>
  );
}
export default Cameras;
